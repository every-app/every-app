/**
 * Token expiry duration for embedded app JWT tokens.
 * Used by both jwt-utils (when issuing tokens) and session-token (when building response).
 */
export const EMBEDDED_APP_TOKEN_EXPIRY_SECONDS = 60;
