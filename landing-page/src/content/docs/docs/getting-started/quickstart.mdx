---
title: Getting Started
description: Get up and running in 5 minutes
---

## Prerequisites

1. [Create a Cloudflare account](https://dash.cloudflare.com/sign-up) (free, no credit card required)

2. Log in to Cloudflare:
   ```bash
   npx wrangler login
   ```

## Self-Host the Gateway

The Gateway is your app hub—it handles auth and gives you a single URL for all your apps.

```bash
npx @every-app/cli gateway deploy
```

Follow the link to create your account.

## Self-Host an Example App

Try one of the example apps to see how it works:

### Todo App

A simple task manager to get started.

```bash
npx gitpick every-app/every-app/tree/main/apps/todo-app every-todo-app
cd every-todo-app
npx @every-app/cli app deploy
```

### Workout Tracker

Track your workouts and programs.

```bash
npx gitpick every-app/every-app/tree/main/apps/workout-tracker every-workout-tracker
cd every-workout-tracker
npx @every-app/cli app deploy
```

### Cooking Assistant

An AI-powered recipe manager (requires OpenAI API key and Cloudflare paid plan since the bundle is too big currently).

```bash
npx gitpick every-app/every-app/tree/main/apps/chef every-app-chef
cd every-app-chef
npx @every-app/cli app deploy
npx wrangler secret put OPENAI_API_KEY
```

Refresh your Gateway and the app will appear.

## Next Steps

[Build your own app](/docs/build-an-app/create-app) - Set up your coding agent and start from a template
