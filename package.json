{
  "scripts": {
    "types:check": "pnpm -r --no-bail run types:check",
    "format:check": "pnpm -r --no-bail run format:check",
    "format:write": "pnpm -r --no-bail run format:write",
    "cf-typegen": "pnpm -r --no-bail run cf-typegen",
    "test": "pnpm -r --no-bail run test",
    "ci:check": "npx concurrently --names \"formatting\",\"types\",\"knip\",\"llms-txt\",\"test\" --prefix-colors \"yellow,blue,magenta,cyan,red\" \"pnpm run format:check\" \"pnpm run types:check\" \"pnpm run knip\" \"pnpm run llms-txt:check\" \"pnpm run test\"",
    "llms-txt:check": "cd landing-page && node scripts/generate-llms-txt.js --check",
    "dev": "npx concurrently --names \"todo-app,every-app-gateway\" --prefix-colors \"blue,green\" \"cd apps/todo-app && pnpm run dev\" \"cd apps/every-app-gateway && pnpm run dev\"",
    "knip": "pnpm -r --no-bail run knip"
  }
}
