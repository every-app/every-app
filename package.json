{
  "scripts": {
    "types:check": "pnpm -r --no-bail run types:check",
    "format:check": "pnpm -r --no-bail run format:check",
    "format:write": "pnpm -r --no-bail run format:write",
    "ci:check": "concurrently --names \"formatting\",\"types\",\"knip\" --prefix-colors \"yellow,blue,magenta\" \"pnpm run format:check\" \"pnpm run types:check\" \"pnpm run knip\" ",
    "dev": "concurrently --names \"todo-app,every-app-gateway\" --prefix-colors \"blue,green\" \"cd apps/todo-app && pnpm run dev\" \"cd apps/every-app-gateway && pnpm run dev\"",
    "knip": "pnpm -r --no-bail run knip"
  }
}
